> **直接替换决定（规范性，2026-08-18）**：当前项目 owner 授权本 change 直接完成 Graph-only repository cutover。所有 production caller、writer/reader、runtime route、public contract 和 persisted authority 直接切到 Graph；不等待前置 change archive、managed-environment owner、backup/drain、rollback point、rollback drill、观察期或外部 sign-off，也不保留 dual mode、feature flag、fallback 或 compatibility facade。legacy persisted input 只允许被隔离诊断为 quarantine，不能被 live reader、resume、replay、worker 或 publication 执行。Artifact subsystem 继续保留，只有 legacy Workflow Artifact bridge/writer/reader 和 `ARTIFACT` leaf classification 可删除。

## 1. 基线、依赖和冻结

直接替换不设置 Gate B/C 外部前置；inventory、owner contract、replacement test、runtime wiring 和 legacy deletion 都是本 change 的 repository-local 工作，按可验证的职责边界分 commit 完成。

- [x] 1.1 记录 owner 决定：本 change 不等待 `harness-workflow-graph-runtime` 归档或其他 change 的 canonical rebase，直接以本 change 的 Graph contract 为唯一实现目标
- [x] 1.2 将 `Legacy Workflow Graph Compilation` 视为本 change 内的直接删除范围，不保留过渡 compiler 或 dual declaration reader
- [x] 1.3 生成机器可读的生产依赖 inventory，覆盖 `framework/workflow` 92 个 tracked files、所有外部 imports、public exports、registries、reflection strings 和 CLI/API/MCP/SDK entrypoints
- [x] 1.4 盘点所有 persisted run manifests、events、checkpoints、replay bundles、artifact indexes、conversation cursors、iteration checkpoints 及其 schema/version
- [x] 1.5 `[Gate A]` 为 inventory 的每一行填写 `keep/adapt/migrate/delete/quarantine`、replacement owner、caller、数据处置、迁移 phase、验证命令和删除 commit
- [x] 1.6 `[Gate A]` 建立 subtract-only architecture freeze gate，以机器可读基线阻止新增 `framework.workflow`、`framework.harness.workflow`、`WorkflowRunner`、`WorkflowExecutor` 和 legacy schema writer；已迁移项只能从基线移除，不能重新加入
- [x] 1.7 记录当前 Research Graph golden definition、normalized graph checksum、gate evidence、terminal manifest 和 offline replay 基线
- [x] 1.8 记录不依赖受管环境、维护窗口或 rollback owner；live legacy records 采用 typed quarantine，不执行环境级数据迁移
- [x] 1.9 审计所有 active `workflow-*`、`*workflow*` OpenSpec changes；仍会同步旧 runtime requirement 的 change 必须改为 superseded/`--skip-specs` 归档或先重写为 Graph contract

## 2. Graph-only 公共契约

以下段落中的 Gate A/B/C 文字是历史提交记录，不再是当前阻塞条件。按顶部直接替换决定，2.3-2.6、2.7、2.8、2.10 和后续 production wiring 直接实现并测试；legacy compiler/reader 在替换覆盖后删除。Task 2.1 的 preflight owner slice 已由 commit `d31fef488f72bdf7aeeba6918fa8fc0bbeda78cc` 完成：纯 normalized-Graph validation 已迁到 `framework/harness/graph/validation`，compiler/`prepare(workflow)` 混合、旧 Workflow validation package/re-export 与 `HarnessPreparedGraph` 已删除且没有 shim；当前任务只剩 live Graph compiler/admission 收口，证据见 `evidence/graph-preflight-owner-contract.json`。Task 2.9 的 root-facade slice 已完成且 Artifact-facing root ports 保留；剩余任务按直接替换继续推进。

Task 2.4 的 Gate A compiler/resolver contract 已由 commit `1f9e20e938fe92fac78e90c8ea2e774f390697fe`（tree `130fc388b9f26c0ce0aed72cdd6209c6ca3720d4`）落地：`HarnessGraphCompiler` 只接受 checksum-valid `HarnessGraphDefinition`，输出 `newsroom.harness-normalized-graph/v2` / `newsroom.harness-graph-compiler/v2`，wire payload 不含 `workflow_id`、`workflow_version` 或 `workflow_ref`，并固定 Graph/definition、leaf、TaskPlan、committed-output、repair 及成功/失败 terminal policy lineage。v2 semantic preflight 不得回退到 leaf-owned repair authority，且 exact registry resolver 已能解析 Graph identity 与独立 failure-terminal handler。现有 production control plane、AgentLoop smoke 和 legacy compiler 仍使用 normalized v1；`HarnessRunSpec.graph`、live admission/runtime composition、durable recovery/replay 与 Gate C 删除均未完成，因此 2.1/2.4/2.7/2.8/2.10 继续开放，任务计数仍为 `28/102`。Artifact owner 完整保留。验证为 focused `28 passed`、Graph package `152 passed`、architecture `33 passed`、mandatory smoke `2709 passed, 23 deselected, 22 warnings`、source validation `true/0/0` 与 OpenSpec strict-all `533/533`；证据见 `evidence/graph-only-compiler-contract.json`。

commit `98c3a97d47860340de6f46599de686e81642a791` 将 pure Graph result-lineage value contract 物理迁至 `framework/harness/graph/result_lineage.py`，删除 control-plane path/re-export 且不留 shim；所有 caller 直导入 Graph owner，root API 从 Graph owner 保持同一 contract。该 slice 只推进 2.1/3.1 owner/import 收敛，不改变 live state/compiler/writer/admission，所以 2.1/3.1 继续开放，证据见 `evidence/graph-result-lineage-owner-relocation-contract.json`。

- [ ] 2.1 在 `framework/harness/graph` 建立最终 Graph namespace，并为 DSL、normalized graph、compiler、reader、versioning、validation、bindings 和 runtime resolution 划分 owner
- [x] 2.2 定义不可变 `HarnessGraphDefinition`，明确 `graph_id`、`graph_version`、root Graph、activity bindings、terminal side-effect policy 和 canonical serialization
- [ ] 2.3 将 `HarnessRunSpec.workflow` 改为 `HarnessRunSpec.graph`，并同步所有内部构造、类型检查、序列化和错误码
- [ ] 2.4 将 `HarnessWorkflowGraphCompiler` 改为 `HarnessGraphCompiler`，只接受显式 Graph definition 并输出带 checksum 的 `NormalizedHarnessGraph`
- [ ] 2.5 删除 `graph=None` fallback、`entry_step_id`、`routing_rules`、`declaration_mode`、legacy schema constants 和 dual declaration reader
- [ ] 2.6 让 preflight 在 `RUN_CREATED`、checkpoint、artifact、worker 或 publication side effect 前拒绝所有 legacy Workflow declarations
- [ ] 2.7 为 Graph schema、compiler、condition、activity binding、gate、checkpoint、event 和 manifest 建立精确 version pinning 及 unknown-version fail-closed 行为
- [ ] 2.8 保留 `HarnessStepSpec` 仅用于 executable leaf lifecycle，禁止它表达 outer routing、node readiness 或 publication decision
- [x] 2.9 更新 `framework/harness/__init__.py`、模块 `__all__` 和 type-checking imports，删除 Workflow 名称的 re-export
- [ ] 2.10 增加 Graph contract round-trip、canonical checksum、unknown construct、missing Graph、dual declaration 和 no-worker-before-preflight 测试

commit `452626dbff266ae58271f81cac7b39b035e0fc56` 将 pure Graph decision contract 物理迁至 `framework/harness/graph/decision.py`，删除 control-plane path/re-export 且不留 shim，并把 contract test 迁入 Graph tests；该 slice 只推进 2.1/3.1 owner/import 收敛，不改变 live scheduler/state/checkpoint/admission，因此 2.1/3.1 继续开放，证据见 `evidence/graph-decision-owner-relocation-contract.json`。

commit `b299beedd979eda4c80a8c47e4ab1170770862c2` 将 pure Graph run-operation contract 物理迁至 `framework/harness/graph/operations.py`，删除 control-plane path/re-export 且不留 shim；该 slice 只推进 2.1/3.1 owner/import 收敛，不改变 live cancellation writer/admission，因此 2.1/3.1 继续开放，证据见 `evidence/graph-run-operation-owner-relocation-contract.json`。

commit `16e0542ebecff5e437ee16843bad8ac891c2a409` 将五个 immutable Graph observability value contract 物理迁至 `framework/harness/graph/observability.py`，root/Graph 只从 Graph owner 导出；`control_plane` 保留读取 `HarnessGraphState` 的 `graph_metric_samples()` 与 `graph_health_report()` projection adapter，不再 re-export value contract。该 slice 只推进 2.1/3.1 owner/import 收敛，不改变 live state/compiler/writer/admission、Artifact authority 或 Gate B release，因此 2.1/3.1 继续开放，任务计数保持 `30/102`，证据见 `evidence/graph-observability-value-owner-contract.json`。

## 3. Harness 控制面迁移

Task 3.3 的 Gate A inactive、checksum-bound Graph phase transition record/strict reader 与 replacement tests 已由 commit `a368baee4d13108a27705ebc3134a5e563550a0d` 落地，但不得据此切换 legacy `phase_recorded` writer。当前 writer 已 durable 且绑定 `node_instance_id`，却仍使用 `newsroom.harness-event/v1` 且缺少 exact `GraphRunIdentity` / `GraphEventContext`；只有 Gate B task 4.11 激活新 writer、验证 record/envelope sequence 一致并完成 recovery/replay 与 projection cutover 后，3.3 才能勾选。证据见 `evidence/graph-phase-transition-contract.json`。

Task 3.7 必须复用已完成的 `framework/harness/side_effects` authority。Research Artifact publication 已真实接入该路径且 Artifact owner/runtime/storage 保留；generic memory 没有 production writer，legacy MCP port 也未进入 production composition。commit `a07a35d0ed9e5a673251e3a427da32f6a3cf69fa` 已完成 Gate A bypass hardening：删除 `MemoryPort.commit_write(candidate)` 与 decision-like candidate statuses，并删除 caller-supplied MCP approval、让 side-effect/unknown Tool 在未绑定 exact Harness handler 时 fail closed。最终 Tool leaf、Graph-only dispatch receipt 与 production composition 尚未切换，因此 3.7 仍保持开放。证据见 `evidence/harness-side-effect-port-boundary.json`。

commit `09f6166f57919aed6426d8d54f9465f28a26206a` 先补齐 Reader Repair 的 inactive exact memory handler 与 atomic commit port：worker prepare 零写入，controller-terminal commit 才能获得 checksummed、idempotent、versioned receipt。commit `f762e7a34bfa238c9cf6fd6aa6b3538766eef180` 再补齐 append-only Postgres commit ledger、single-transaction case/strategy writes、canonical advisory locks、rollback/crash recovery、严格 receipt mapping 与 inactive env factory；production handler/worker registration、legacy `ReaderRepairService` direct-write removal 和 Graph-only run admission 仍未完成，故 3.7/5.5/5.7 继续开放。证据见 `evidence/research-reader-repair-memory-side-effect-contract.json`。

Reader Repair worker 审计确认现有 8-activity v1 declaration 不能直接接 production worker：它没有 typed patch application activity、真实 after-payload candidate、deterministic application verification record 或 Harness node-output binding；legacy service 唯一 after ref 仍由 `f"{payload_id}:repaired"` 合成。5.5/5.7 在 registration 前必须先升级 execution contract，禁止 worker 自报 `passed`、拼接 repaired ref 或把 candidate ref 当 durable/public ref；failed repair memory 还需要独立 controller-terminal failure policy，不能绕过 failed gate。证据见 `evidence/research-reader-repair-execution-gap-audit.json`。

Reader Repair v2 Gate A execution contract 已由 commit `196a61be3d89511da133324c6df282856562b31c`（tree `a9f8eec50adf80112af6105120cd3fcfe6bb64ab`）补齐 bounded discriminated patch operations、纯内存 application candidate、双输入绑定 observation candidate、canonical deterministic verification record、4 个 exact gate adapters 与 2 个 candidate-only Function worker builders；stale/no-op/open patch、嵌套 source scope 扩权、Artifact lineage 篡改、worker substitution 和自然语言自报通过均 fail closed。该 slice 未改动或注册 `research.reader_repair.graph@1`，也没有解决 Harness durable node-output identity、result contract、failure topology、legacy direct writes 或 production composition，因此 3.6/3.7/5.5/5.7 均继续开放，任务计数仍为 `25/102`。

通用 committed-output Gate A contract 已由 commit `2462b2e5f316755d989ba4f943990589bcdae7c2`（tree `4d5b22ad35244a0f16345092254b6e09c0e29fd7`）随 strict `HarnessGraphDefinition` v5 落地：definition 固定 exact producer/consumer activity 与 node identity、output key 和独立 receipt input key，并拒绝非确定性 producer-before-consumer topology 与 loop-local iteration ambiguity；`HarnessCommittedNodeOutputReceipt` checksum 绑定 definition、producer contract、Graph/node resource、完整 immutable commit/candidate 与 output ref；inactive resolver 只从 `HarnessNodeOutputResourcePort` 读取当前 commit 并重算 payload checksum。Reader Repair v1 仍声明空 binding，Graph-only compiler lineage、Graph v2、result receipt、live physical executor 和 bounded failure topology 均未完成，因此 2.4/2.10/3.6/4.13/5.5/5.7 均继续开放，任务计数仍为 `25/102`。验证结果为 focused `98 passed`、mandatory smoke `2613 passed, 23 deselected, 22 warnings`、source validation `true/0/0` 与 OpenSpec strict-all `533/533`；证据见 `evidence/harness-committed-node-output-binding-contract.json` 与 `evidence/research-reader-repair-execution-gap-audit.json`。

Reader Repair Graph v2 的 inactive Gate A slice 已由 commit `107fa2e8eee999da3cd9428967c90fcadbb4d246`（tree `561edd064f5dd2c7eb971ba8a0c09ac396f2d3e1`）落地：`research.reader_repair.graph@2` 固定 10 步 typed apply/observe/verify/result 主链、1 条 exact application committed-output binding、4 条 definition-owned bounded repair routes、最多 2 次 SubAgent local attempt，以及只接受完整 receipt 和重算 verification 的 `ReaderRepairCommittedResultGate@1`。成功 result 绑定真实 after-payload checksum、application/verification 和 definition/resource/commit/output/receipt identity，不生成 public/Artifact ref。live resolver/executor injection、definition-to-normalized compiler lineage、production registry/composition/admission、legacy synthetic ref/direct memory write 删除与 separate failure-terminal policy 仍未完成，所以 2.4/2.10/3.6/3.7/4.13/5.5/5.7 保持开放且任务计数仍为 `25/102`。验证为 focused `29 passed`、boundary `97 passed`、architecture `194 passed`、mandatory smoke `2618 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533/533`；证据见 `evidence/research-reader-repair-graph-v2-contract.json`。

Reader Repair v2 的 inactive exact runtime binding bundle 已由 commit `f61fcf8f3655d17813a82b15a8abe40139befb31`（tree `e03b48c78578fb1775661224703b1ed4499192b7`）补齐：`build_reader_repair_runtime_binding_bundle()` 只接受与 10 个 Graph activity 精确闭合、零缺失且零多余的 worker/activity implementation maps，复用 `HarnessRuntimeBindingAuthority` 校验 exact ref、version、canonical worker type、leaf kind、activity contract、全部 declared gate 与唯一 Reader Repair memory side-effect handler。memory handler 必须同时实现 worker-origin `prepare()` 和 controller-terminal `commit()`；bundle manifest 固定声明 builder 自身不安装 runtime authority，且不包含 Artifact handler。`single_paper_runtime.py` 与 `interfaces/composition/research.py` 均未导入或调用该 builder，真实 Function/SubAgent/activity implementations、Graph-only compiler/admission、live receipt injection、production authority installation 与 legacy direct-write cleanup 仍未完成，所以 3.6/3.7/5.5/5.7 继续开放，任务计数仍为 `25/102`。验证为 focused `9 passed`、Reader Repair/Research boundary `109 passed`、Harness Graph/node-output `143 passed`、architecture `196 passed, 4 warnings`、mandatory smoke `2627 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533/533`；证据见 `evidence/research-reader-repair-runtime-binding-contract.json`。

Reader Repair v2 的 8 个真实确定性 Function worker 已在后续 inactive Gate A slice 中补齐：`build_reader_repair_function_worker_implementations()` 从当前 definition 精确构造 detect/context/apply/verify/result/case/strategy/memory workers；只读 recall port、composition-owned run authority/UTC timestamp、durable committed-output resolver、跨 run/scope/attempt/task-input fail-closed 和 canonical replay 输出均已覆盖。`build_repair_result` 显式增加 `reader_payload` business input，当前 checksum 更新为 `sha256:d14a1951a8493de4366e83e28c417cf9a5c68d6bdbbe60d67fc4d411f8544560`，receipt 仍只能从独立 durable binding 取得。10 步测试以真实 node-output resource 通过全部 gate，memory worker 只产生 intent；factory 未进入 Graph facade 或 production composition，也不包含 Artifact authority。因此剩余 SubAgent/activity implementation、live executor/receipt wiring、Graph-only compiler/admission、production installation、legacy cleanup 和 failure-terminal policy 仍使 3.6/3.7/5.5/5.7 保持开放，任务计数仍为 `25/102`；证据见 `evidence/research-reader-repair-function-worker-contract.json`。

Reader Repair v2 的两个真实 candidate-only SubAgent worker 已由 commit `c9f4b3f6f7e9fc3b36cac28f9b24412fa073e53b`（tree `fb44a8192161a250fa8363367c1ff771d5460630`）补齐：factory 精确闭合 proposer/verifier 两个 Graph leaf 与 `SubAgentSpec`，通过 `ResearchCandidateWorkerPort` 生成候选，并由确定性代码拥有 candidate/operation/application identity、component checksum、input bindings 和 observation source projection。跨 run、错误 worker/step/input、LLM 自带 controller fields 或自报 verdict 均 fail closed；10 步测试现在执行 8 个真实 Function 与 2 个真实 SubAgent worker，并继续通过全部 deterministic gates。factory 未进入 facade/production composition；在该 SubAgent 提交时，structured provider adapter 尚未支持两个 Reader Repair task，activity implementation、live executor/receipt wiring、Graph-only admission、production installation、legacy cleanup 与 failure-terminal policy 仍开放，因此 3.6/5.5/5.7 不勾选且任务计数仍为 `25/102`；证据见 `evidence/research-reader-repair-subagent-worker-contract.json`。

Reader Repair provider-backed structured candidate 的 inactive Gate A slice 已由 commit `9e41c7e714254bf3119e9a918ffc748763088eb3`（tree `7216849e974b3cc88d1fb9d735f21c34eae2f4ea`）补齐：两个 task 的 strict provider schema 由 `business/research/ports/reader_repair_candidate.py` 持有，`StructuredResearchCandidateWorker` 仅通过 exact port contract 消费；schema 递归移除 metadata/controller-owned fields、关闭所有 object 的 `additionalProperties`，并把 table rows、analysis quality 与 evidence coverage 转为 bounded entries projection。prompt 只包含 bounded/redacted Reader Repair 投影，Artifact/control/credential fields 不进入 prompt，source scope 从实际投影派生，超出或已被截断的 ref 均 fail closed；SubAgent worker 只确定性还原 entries，重复 key 被拒绝。该能力仍未注册 Reader Repair production worker/runtime authority，也没有 Artifact handler/publication policy，因此 activity implementation、live executor/receipt wiring、Graph-only admission、production installation、legacy cleanup 与 failure-terminal policy 仍开放，3.6/5.5/5.7 不勾选且任务计数保持 `25/102`；证据见 `evidence/research-reader-repair-structured-candidate-contract.json`。

Reader Repair 独立 failure-terminal Gate A contract 已由 commit `faec9fa7d4be80a05df5428452c3ac0791fbb663`（tree `ee6ee83d86cc1d40eaababf99a03a6b2dc42a397`）补齐：GraphDefinition strict schema v6 声明与成功 policy/handler 不同的 exact failure policy；通用 terminal record checksum 绑定相邻 durable decision/projection、失败 node、attempt/replan 与 gate evidence，并覆盖失败 `COMPLETE_RUN(graph_terminal_failure)` 和 `HALT_RUN(verification_failed_replans_exhausted)` 两种真实终态。Reader Repair candidate/handler 只接受完整 issue/candidate/application/observation/verification lineage、failed gate evidence、controller-terminal causation 和 `quarantine` authority；独立 Postgres adapter 原子写一条 `successful=false`、无 `payload_after_ref`、零 strategy/public/Artifact ref 的 diagnostic case。runtime bundle 现在校验 success 与 failure 两条不同 handler registration，当前 Graph checksum 为 `sha256:fadbddd1dfb4e0880745f23e0be136a449ae23cd92d2b855c43be17f1a5d9307`，但仍不安装 runtime authority；production controller emission、Graph-native executor/receipt wiring、composition、Graph-only admission 和 legacy direct-write cleanup 尚未完成，早期失败也不得伪造 domain lineage。因此 3.7/5.5/5.7 保持开放、任务计数仍为 `25/102`；Artifact subsystem 保留且 Reader Repair 无 Artifact publication handler/policy。验证为 failure focused `8 passed`、architecture/inventory `14 passed`、mandatory smoke `2651 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533/533`；证据见 `evidence/research-reader-repair-failure-diagnostic-contract.json`。

通用 Graph-native physical executor Gate A contract 已由 commit `b0756dd8b9683c3eb0d0b19b4dc9e283a71ebde8`（tree `a0b46eece9ab8cd48f25308c77f8e74c7f00a900`）补齐：`HarnessGraphPhysicalActivityExecutor` 只消费 durable `HarnessGraphActivity`，通过 strict checksum-bound input resolver 与 composition-owned exact binding authority 完成 pair/leaf/capability admission，并在 admission 后复用 node-output resource adapter；worker 只接收 `harness_graph_activity`，正常 output/result 与 original activity/resource/output keys 绑定，output-committed/result-interrupted 可无 worker 重跑恢复。rejection 不取 lease 或提交 result，superseded attempt 不提交 output/result，indeterminate attempt 不提交正常 output；executor 不调用 legacy activity `dispatch(request)`，没有进入 production composition，也不导入或注册 Artifact handler/publication policy。Task 4.13 继续开放，等待 live dispatcher、受管 durable input/result/node-output resource、receipt injection、cancellation/reconciliation 与 production authority。验证为 focused `15 passed`、broader `258 passed`、mandatory smoke `2662 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533/533`；证据见 `evidence/harness-graph-physical-activity-executor-contract.json`。

Task 3.9 的 Graph-only Gate A contract 已由 commit `621a9198f762754e870250a822a4ac7cdef1fe91`（tree `df11818350767708795969638bfe090785e70fe4`）补齐。`HarnessGraphReference` 现在按 normalized schema 严格区分 legacy `workflow_ref` 与 v2 `graph_ref`，v2 state、decision、checkpoint 分别使用 exact `/v2` schema，并与 `newsroom.harness-graph-compiler/v2`、condition policy 和 Graph checksum fail-closed 配对；v2 wire payload 不携带 `workflow_ref`。测试在 decision 已 durable commit、projection 尚未写入时创建新 runtime，证明 crash recovery 只消费 pinned Graph/history 即可补投影；checkpoint 从 verified prefix 离线 replay，pinned kernel 不调用 worker。篡改 `gate:0000` 版本会触发 replay mismatch，成功终态缺少 evidence 会映射为 `graph_history_evidence_missing` quarantine。production smoke 仍为 normalized v1，本 slice 未切换 writer/reader/compiler/runtime authority，也未改变 Artifact owner/runtime/storage/publication。任务计数更新为 `29/102`，证据见 `evidence/graph-only-recovery-replay-contract.json`。

Graph-only TaskPlan stage-binding 的 Gate A contract 已由 commit `335723910dc96e8c50a53945d6b86499474305cf`（tree `64706cefa20b623d5dcde401f22e743bc07444bc`）完成。`TaskPlanStageBinding` 现在按 normalized schema 选择 v1/v2：legacy v1 projection 与 checksum oracle 保持不变；v2 固定 `newsroom.harness-task-plan-stage-binding/v2`、exact `graph_ref`、Graph/compiler/condition/checksum 和 definition-derived stage authority，且不输出或允许读取 `workflow_id` / `workflow_ref`。strict reader 拒绝 mixed schema、Workflow alias、checksum 篡改、跨 Graph 恢复和非 `graph_definition` binding source。真实 Research dynamic GraphDefinition 已通过 `HarnessGraphCompiler` 编译并 round-trip v2 binding；production run admission/composition 与 durable TaskPlan candidate/plan/event/checkpoint/store/replay 仍使用 legacy identity，所以 2.7/5.4 保持开放，任务计数仍为 `29/102`。Artifact subsystem 完整保留，证据见 `evidence/graph-only-task-plan-stage-binding-contract.json`。

Task 2.1/3.1 的 Gate A reference-owner slice 已由 commit `0b3c873945c32e12c199d77846ec692f1ae2b2a2`（tree `09869149f74e96fbf475d88d8daf2c1725590aab`）完成：`HarnessGraphReference` 的 concrete definition 已迁入 `framework/harness/graph/reference.py`，Graph package 与 root facade 指向同一 class，旧 `control_plane`/`graph_state` export 和全部具体 caller import 已删除且没有 shim；v1/v2 wire、checksum、version pinning 与 diagnostics 保持不变。scheduler/evaluator/state/checkpoint/durable events 等 module 尚未整体迁入 Graph namespace，production authority 仍为 normalized v1，TaskPlan durable identity、Gate B writer/reader 和 Gate C compiler deletion 也未完成，因此 2.1、2.7、3.1 保持开放，任务计数仍为 `29/102`。Artifact owner 与 publication path 未改动，证据见 `evidence/graph-reference-owner-contract.json`。

Task 5.4 的 Graph identity 前置 slice 已由 commit `0ece8f00438ec472d57d10611f00e2ec832c75c4`（tree `319a9c927a2335e3b5c21229d5db2c81d7118836`）完成：`TaskPlanStageIdentity` 由 frozen `TaskPlanStageBinding` 唯一派生并严格区分 v1 Workflow identity 与 v2 exact Graph identity；`PlanBuildRequest`、`TaskPlanValidationContext`、`TaskPlanStageRequest` 已共享该 contract。legacy request wire 保持不变，v2 request 不输出 Workflow alias，mixed schema、tamper、cross-Graph restore 与 legacy candidate 注入均 fail closed。candidate/validated plan/result/event/checkpoint/queue/store/replay v2 schema、Research production composition 和 Gate B activation 仍未完成，所以 2.7/5.4 保持开放，任务计数仍为 `29/102`。Artifact subsystem 完整保留，证据见 `evidence/graph-only-task-plan-stage-identity-contract.json`。

Task 5.4 的 PLAN contract 增量 slice 已由 commit `6412fb1107515dd7a2f923aab0ceac1158dfd26c`（tree `4432245c032fc535276a08e3d38208b7836d0def`）完成：`PlanCandidate` 与 `ValidatedTaskPlan` 新增严格 Graph-only v2 schema，v1 writer/wire/checksum 不变；v2 固定 exact Graph/stage identity 且不允许 Workflow alias，Research dynamic GraphDefinition 固定 plan `/v2`，builder、validator、accept 和 patch 都共享 frozen `TaskPlanStageIdentity`。mixed schema、tamper、cross-Graph 注入均 fail closed；现有 v1 event/store 在任何 mutation 前以 `graph_task_plan_event_schema_unavailable` 拒绝 v2。result/event/checkpoint/queue/projection/durable store/recovery/replay、production Research composition 与 Gate B activation 仍未完成，所以 2.7/5.4 保持开放，任务计数仍为 `29/102`。Artifact subsystem 完整保留且 publication authority 未变，证据见 `evidence/graph-only-task-plan-candidate-plan-contract.json`。

TaskPlan result contract 的 Gate A 子切片已由 commit `e7ec9d9c00ec63cd76883d31719a64415d39d215`（tree `85c581dc66edaf2ca67af15fdd9a859cef3d6e4a`）落地：result `/v3` 从 accepted Graph-only plan/task 派生 exact Graph/stage/plan/worker/binding identity，不含 Workflow alias；strict reader、tamper/cross-Graph tests 与 legacy v1/v2 checksum oracle 已覆盖。verifier 和 SubAgent transcript identity 仍要求 `workflow_id`，因此两个 store 在任何 event/artifact/projection mutation 前以 `graph_task_plan_result_runtime_unavailable` 拒绝 v3。task lifecycle、checkpoint、queue、recovery/replay 与 production composition 尚未迁移，2.7/5.4 保持未勾选，计数仍为 `29/102`。Artifact subsystem 与 publication authority 不变，证据见 `evidence/graph-only-task-plan-result-contract.json`。

上述 verifier/transcript blocker 已由 commit `2497774265449566f3ef9fe482f9835e04892033`（tree `0dd80ac90eb0fd028f7df291e277a92ca42dcfe6`）的后续 Gate A slice 关闭：SubAgent identity/context/output/transcript/receipt/bundle 新增 strict Graph-only v2，receipt 绑定完整 identity checksum，v1 wire/ref/checksum oracle 保持不变；`TaskPlanResultVerificationRequest` 现在必须携带 accepted plan、exact task 与确定性 `TaskInstance`，verifier 重建 attempt 并逐字段校验 transcript Graph lineage，再经 `TaskResultRecord.for_plan()` 产生 legacy v2 或 Graph-only v3 result。当前 `SubAgentInvocation`、runtime adapter、task lifecycle event/result store、checkpoint、queue、projection、recovery/replay、Research production composition 与 run admission 尚未迁移，所以 store 继续 fail closed，2.7/5.4 仍不勾选，计数保持 `29/102`。Artifact subsystem 与 publication authority完整保留，证据见 `evidence/graph-only-task-plan-verifier-transcript-contract.json`。

上述 invocation/runtime-adapter blocker 已由 commit `b0bff64ac38601304865aee05b0e48b8518acbe7`（tree `9093198c0eda21da4457aeef587b99e81189d6a6`）的后续 Gate A slice 关闭：`SubAgentInvocation` 新增 Graph-only v2，`ResolvedSubAgentTaskAdapter` 只从 accepted plan、exact task/binding 与 deterministic instance 派生 invocation/attempt identity；runtime 自动写入并恢复 v2 transcript evidence，common result adapter 固定 node-result/materialized-bundle/producer revision v2，Research materializer 只接受 plan-owned Graph identity。legacy invocation wire/checksum 与 production v1 composition 保持不变，mixed schema/legacy Graph identity 注入 fail closed。Graph-only task lifecycle event/result store、checkpoint、queue、projection、recovery/replay、production Graph-only composition 与 run admission 仍开放，store 继续以 `graph_task_plan_result_runtime_unavailable` 拒绝激活，因此 2.7/5.4 仍不勾选、计数保持 `29/102`。Artifact subsystem、真实 materialization path与 publication authority完整保留，证据见 `evidence/graph-only-task-plan-invocation-runtime-contract.json`。

上述 lifecycle/result-store blocker 已由 commit `c933d67f5e50082cdd184fdd8b284a09b249be9f`（tree `6d7847981d4c6902666e2c138254b29b182fb8f3`）的后续 Gate A slice 关闭：TaskInstance、TaskProjection 与 TaskPlanProjection 新增 Graph-only v2 schema并拒绝 Workflow alias；全部 lifecycle event 从 accepted plan 派生 exact identity，in-memory/durable store 在合法 reservation 后持久化 v3 result、`TASK_RESULT_ACCEPTED` 与 `TASK_COMPLETED`，并在 reopen 时闭合 event/projection/plan/result identity。`graph_task_plan_result_runtime_unavailable` 仅保留为历史证据，不再是当前 blocker。queue、checkpoint、replay、recovery、Graph-only context major schema、production Research composition 与 run admission 仍开放，所以 2.7/5.4 继续未勾选、计数保持 `29/102`。Artifact subsystem 完整保留，TaskPlan immutable documents 仍走既有 Artifact port，publication authority 未迁移；证据见 `evidence/graph-only-task-plan-lifecycle-result-store-contract.json`。

上述 checkpoint/replay blocker 已由 commit `026ae84f21d061b6ca32ec7152bfc43b2addf70d`（tree `e204af705528768336f59f10d42ce28c4cd3486c`）的后续 Gate A slice 关闭：checkpoint/replay 新增 strict Graph-only v2，完整绑定 Graph/stage/plan/projection/attempt/result/event-history identity，不含 Workflow alias；offline replay 不调用 worker、不合成缺失 terminal evidence，也不执行 publication、memory、tool 或 external side effect。Graph replan 同时使用 exact `PlanPatch /v2`，validator、两个 store 与 replay 均拒绝 v1-to-v2 mixed authority、tamper、cross-Graph patch 和 cross-Graph new plan，legacy v1 wire/checksum 不变。queue 仍以 `graph_task_plan_queue_contract_unavailable` 关闭，Graph recovery 在 queue/reclaim continuation 完成前对所有 lifecycle shape 统一以 `graph_task_plan_recovery_contract_unavailable` 前置关闭；production Graph-only composition/run admission 仍未激活，所以 2.7/5.4 继续未勾选、计数保持 `29/102`。Artifact subsystem 与 publication authority 完整保留；证据见 `evidence/graph-only-task-plan-replay-checkpoint-contract.json`。

上述 queue/recovery blocker 已由 commit `0ae73855bf647c4f37c36cec958b85e06931ad03`（tree `1ea90ef6ddcaa5b883fd1202fe136b5530a3d4ce`）的后续 Gate A slice 关闭：Graph-only `TaskPlanQueueProjection /v2` 是 strict identity-only generic task，durable `TaskPlanQueueReadback /v2` 只能由构造时注入的 `TaskPlanQueueReadPort` 返回，`TaskPlanRecoveryService` 不再接收 caller-supplied readback 或 bare queue ids。Redis adapter 用只读原子 snapshot 区分 undelivered、PEL pending 和 acknowledged record；pending/acknowledged/scan-incomplete 都 fail closed，DISPATCHED/RUNNING 只输出无执行 authority 的 `await_stale_reclaim` continuation。没有 worker registration、enqueue、lease、reclaim 或 production composition 接线；真实 Redis integration 已加入但本次因 `NEWS_TEST_REDIS_URL` 未设置而 skipped。Graph-only ContextEnvelope、Research/run admission、生产 queue/lease/reclaim installation、Gate B/C 仍开放，因此 2.7/5.4 不勾选、计数继续保持 `29/102`。Artifact subsystem 与 publication authority 完整保留；证据见 `evidence/graph-only-task-plan-queue-recovery-contract.json`。

- [ ] 3.1 将 control plane、scheduler、evaluator、state、checkpoint、durable events、compensation 和 TaskPlan 的 imports 切换到 `framework.harness.graph`
- [ ] 3.2 将 route table、condition table、activity readiness、quality gate 和 budget context 改为从 pinned Graph 读取
- [ ] 3.3 保证所有 phase transition 继续记录 durable `PLAN -> EXECUTE -> VERIFY` transcript/event，并绑定 Graph/node-instance identity
- [x] 3.4 将 TaskPlan 限定为冻结 Graph 中显式注册 dynamic stage 的内部计划版本，不允许它修改 outer Graph 或新增未注册 worker
- [x] 3.5 将 Sequence、Choice、Parallel-All/Any、Join、Bounded-Loop、Wait、approval、timer、signal、compensation 保持为确定性 control nodes
- [ ] 3.6 将 Function、Tool、Skill、Subagent、AgentLoop 注册为 leaf activity bindings，并让 worker 输出只进入 candidate/evidence channel
- [ ] 3.7 将 memory write、tool authorization、artifact publication 和 external side effect 都接回 Harness-owned gate/port authority
- [x] 3.8 增加 adversarial tests，证明 worker route suggestion、LLM self-score、queue readiness 和 business service cannot alter Graph decisions
- [x] 3.9 增加 crash recovery、checkpoint replay、gate version pinning 和 missing terminal evidence 的 Graph-only tests
- [ ] 3.10 将 `HarnessRunResult`、gate context、Wait/resume、side-effect request 和 inspection/read model 改为直接消费 Graph run/node-instance state，删除 `_graph_compat_state()`、flat `HarnessState` projection、`LEGACY_UNBOUND` 和 synthetic legacy approval identity
- [ ] 3.11 将 SubAgent production invocation/context/output/transcript/receipt/bundle writer、reader 和 store 固定为 Graph-only v3；把 v1/v2 识别移出 production import，删除 live defaults、`is_graph_only` dual branches 和 `workflow_id` authority
- [ ] 3.12 将 `BusinessContext`、`TraceContext`、propagation carrier、tool inspection metrics 和所有相关 caller 迁到 versioned Graph identity；旧字段不得作为 nullable alias 保留
- [ ] 3.13 从 Event schema catalog 删除 Workflow event/operation 的 current registration，删除旧 `Event`/`EventEnvelope` facade 和 production-importable migration application/service/CLI surface；Graph event/read model 成为唯一 live contract
- [ ] 3.14 将 Memory scope/policy/adapter、Governance budget scope、Worker task/result、Skill run context、LLM structured-output policy 和 observability metadata 统一迁到 Graph run/node/stage identity，并同步 PostgreSQL、Research 和 service caller
- [ ] 3.15 删除无 production caller 的 flat `HarnessCheckpoint`、checkpoint store、durable state、replay runtime 及 root/ports exports；保留 control-plane Graph checkpoint/replay owner
- [ ] 3.16 删除 `WorkflowArtifactRef`、`WorkflowArtifactPublisher`、`LocalArtifactPublisher` 及仅服务它们的测试/export；保留 `ArtifactManager` raw storage/integrity/path-safety primitives 和 `framework/harness/artifacts` terminal authority
- [ ] 3.17 收敛 Graph version registry，只保留 live Graph schemas；删除 `condition_from_legacy_dict`、legacy graph schema current constants 和 preflight flat metadata fallback
- [ ] 3.18 将 Harness RAG session/spec、context pack 和 Research RAG callers 统一绑定 exact Graph identity，删除 nullable Workflow/session identity fallback，并保留 independently owned RAG session capability

## 4. Domain-neutral 能力迁出旧 runtime

本节直接完成 owner、caller、writer/reader 和 runtime wiring。inactive adapter 只保留在测试或历史诊断边界；不得把它当成 live fallback。Artifact owner/runtime/storage/publication 继续保留。

- [x] 4.1 以已落地的 `framework/harness/artifacts/{catalog,governance,reporting,runtime}` 为目标 owner，保留 Graph catalog、quota、usage ledger、GC、cost reporting、alerts 和 lifecycle authority，并补齐 Graph terminal manifest、artifact ref、checksum、path boundary、publication metadata 和 strict content reader contract
- [x] 4.2 迁移 `framework/agent/artifacts/runtime/manager.py`、Research publisher、`infrastructure/research/graph_artifact_lifecycle.py`、`interfaces/composition/research_graph_artifacts.py` 和 artifact interface caller 到 artifact-owned Graph terminal manifest/hash contract，删除 live `framework.workflow.runtime.manifest`/Workflow inspector 依赖
- [x] 4.3 将 event projection、event migration primitives、stream identity validation 和 read models 迁到 `framework/events`
- [x] 4.4 `[Gate A]` 建立 event migration/projection application ports、inactive Graph projection adapter、typed legacy-history diagnostic 和 replacement tests；interface 新路径只依赖 application port，但不得切换 production reader/writer 或 external projection schema
- [x] 4.5 将 cancel、signal、approval、resume、inspect、replay 和 run operation 组合到 Harness Graph application services
- [x] 4.6 将 artifact inspection 改为读取 Graph terminal manifest，并对未迁移历史返回 typed quarantine/history diagnostic
- [x] 4.7 `[Gate A]` 建立消费 Graph run/event contracts 的 artifact/event index owner contract、inactive adapter、dry-run 和 read-back tests，保持路径、checksum、sequence 和 idempotency 防护且不切换 production index pointer
- [ ] 4.8 迁移 `scripts/dev.py`、`interfaces/services` 和 `infrastructure/research` 的所有 live 旧 runtime imports，并删除 fallback
- [x] 4.9 为每个迁出职责添加 owner-level contract tests，证明既有 catalog/quota/usage/GC/cost/alert 行为保持不变，且 artifact owner、lifecycle、composition 和 caller 均不反向导入 `framework.workflow`
- [x] 4.10 `[Gate A]` 建立 Graph node-instance output resource owner、原子 monotonic lease、staged write/commit、typed stale-owner rejection 与 inactive admitted-activity adapter contract；资源世代不得来自 Graph/local attempt、budget、retry credit 或 event sequence
- [ ] 4.11 激活 Harness durable Graph context writer，并将 event projection、run inspection 与 projection schema 切到 Graph event application port；删除旧 reader，不保留回退
- [ ] 4.12 激活 Graph artifact/event index writer/readers，完成 read-back 与 replay checks 后直接切换 Graph index authority；不实现 pointer rollback
- [x] 4.13 将 Graph-native physical executor 安装为 live dispatcher，接入 durable input/result ports、node-output resource/current-commit reader、committed receipt injection 与 cancellation/reconciliation authority；`DataBuffer` 只在 replacement coverage 后删除

## 5. Research Graph 化

- [ ] 5.1 将 `business/research/workflows` 迁为 `business/research/graphs`，同步 builder、module、fixture、test 和 import 名称
- [ ] 5.2 将 paper analysis composition 返回 `HarnessGraphDefinition`，移除 `HarnessWorkflowSpec` 和 legacy routing fields
- [ ] 5.3 将 static Research path 固定为 Graph `Parallel-All + VerifiedAggregation`，验证 Graph checksum 与 gate version
- [ ] 5.4 将 dynamic analysis 作为 frozen Graph 内 opt-in dynamic stage，保留 bounded `PLAN -> EXECUTE -> VERIFY` 和 durable TaskPlan
- [ ] 5.5 将 reader repair subagent declarations、memory write intent 和 promotion boundary 接到 Graph activity/gate contract
- [ ] 5.6 更新 `interfaces/composition/research.py`、Research application service 和 runtime wiring，确保 `business/research` 不导入任何旧 Workflow namespace
- [ ] 5.7 增加 Research static、dynamic、reader repair、gated failure、artifact publication、replay 和 production-adapter composition tests
- [ ] 5.8 运行 Research import boundary scan，确认旧 `paper_radar`、interface、infrastructure、generic Workflow runtime 均无直接依赖

TaskPlan event/store 的 Gate A 子切片已由 commit `7c7f648eca28b2af4989ecd8414279fcf49b9197`（tree `18ffdf7ac460f37533beb85639f736bdf17ec06d`）落地：event v2 固定完整 Graph/stage identity，Graph canonical envelope 不携带 `workflow_id` / `step_id`，in-memory 与 durable store 可写入并 reopen Graph-only candidate/accepted plan；v1 仍是默认 writer 且 wire/checksum 不变。TaskResultRecord、task lifecycle、checkpoint、queue、recovery/replay 与 production Research composition 尚未迁移，所以 2.7/5.4 保持未勾选，计数仍为 `29/102`。Artifact subsystem 完整保留，candidate/plan/projection documents 继续走既有 Artifact port，证据见 `evidence/graph-only-task-plan-event-store-contract.json`。

TaskPlan result contract 的后续 Gate A 子切片已由 commit `e7ec9d9c00ec63cd76883d31719a64415d39d215`（tree `85c581dc66edaf2ca67af15fdd9a859cef3d6e4a`）落地：`TaskResultRecord /v3` 固定 accepted Graph plan/task identity 并拒绝 Workflow alias、tamper 与 cross-Graph substitution，legacy v1/v2 wire/checksum 不变。由于 verifier 与 SubAgent transcript identity 尚未共同迁移，in-memory/durable store 对 v3 result 在 mutation 前 fail closed，task lifecycle 与 production runtime 未激活。2.7/5.4 仍不勾选，计数仍为 `29/102`；Artifact owner、Artifact port 与 publication authority 完整保留，证据见 `evidence/graph-only-task-plan-result-contract.json`。

## 6. AgentLoop 边界和运行证据

AgentLoop LLM call Artifact 的 Gate A owner/receipt slice 已在 `framework/harness/agent_loop/artifacts.py` 建立：strict context/record/receipt checksum 绑定 Graph run/node/activity/checkpoint identity，artifact-owned port 执行稳定幂等写入、二次脱敏、ref 校验与 exact read-back，worker evidence 只返回 candidate refs；`required_for_publication=false` 且 recorder 不具备 manifest/publication API。architecture gate 保证 `framework/agent` 不反向依赖 Harness，并阻止 production layers 激活该 adapter。commit `4a5a4ce57d0e601dbff6879af106215f1dc93de1`（tree `730ade6770458a2ef14466abab1e20c954c316f6`）验证为 focused `10 passed`、扩大回归 `105 passed`、mandatory smoke `2672 passed, 23 deselected, 22 warnings`、source validation `true/0/0` 与 OpenSpec strict-all `533/533`。该提交捕获时，checkpoint context、production input resolver/live composition、Graph activity binding、`AgentRunner` target-path、Graph cursor/iteration checkpoint、Wait registration 和 node outcome/manifest acceptance 均未完成，因此当时 6.1-6.4 均保持开放；后续状态由下方增量记录覆盖。Artifact subsystem 保留且未被移除，证据见 `evidence/agent-loop-graph-artifact-contract.json`。

Graph activity checkpoint task context 的 Gate A contract 已由 commit `273e9fe87886651abd7c130785d63cf7aca47a38`（tree `52d3e79911c5941e152211074c1e39731262be0f`）补齐：execution input strict v2 把 resolver 提供的必需 `graph_checkpoint_ref` 纳入 checksum，physical executor 注入 strict `HarnessGraphActivityTaskContext` v1，durable event 与 worker 共用 `HarnessGraphActivity.from_dict()`，Reader Repair 校验 typed context 的 run/graph/node identity，AgentLoop Artifact 从同一 context 派生 checkpoint/activity identity；旧扁平 context 没有 compatibility fallback，Artifact subsystem 保留且没有新增 publication authority。该提交捕获时仍没有 production input resolver/live composition、`AgentRunner` binding、Graph cursor/iteration checkpoint、Wait registration 或 node outcome/manifest acceptance，所以当时 4.13 与 6.1-6.4 保持未勾选，任务计数为 `25/102`；后续状态由下一段覆盖。验证为 focused `24 passed`、durable/replay `65 passed`、runtime/AgentLoop `137 passed`、architecture `200 passed, 4 warnings`、mandatory smoke `2674 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533/533`；证据见 `evidence/harness-graph-activity-task-context-contract.json`。

AgentLoop Graph conversation state contract 已由 commit `1307e76e9e3a20c598f222a1a08f5e1d9e9d31fb`（tree `97c63f8f8712315c340fd614f227a8c6ab69a2fe`）补齐：live cursor/checkpoint 分别使用 `newsroom.graph-conversation-cursor/v2` 与 `newsroom.graph-agent-iteration-checkpoint/v2`，outer identity 严格绑定 `run_id`、`node_instance_id`、`graph_checkpoint_ref` 且必须全有或全无；显式 resume 只接受与 caller Graph identity 一致的 cursor/checkpoint，metadata identity alias、unversioned legacy payload 与 history-only migration v1 payload 均不能进入 live reader。Local JSON/PostgreSQL state JSON round-trip、redaction、offline legacy migration separation 与 replacement architecture tests 已覆盖，task 6.3 因此完成。production Graph AgentLoop activity binding、durable Wait、node outcome/terminal manifest acceptance、live resolver/composition 与 PostgreSQL conversation parent-table Graph cutover 仍开放；`AgentLoopStepRunner` 仅作为 Gate C 前 legacy runner 保留。Artifact subsystem 继续保留，未新增 publication authority。验证为 focused `50 passed`、扩大 Agent/conversation/migration/architecture `310 passed, 4 warnings`、mandatory smoke `2677 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533/533`；当前任务计数为 `26/102`，证据见 `evidence/agent-loop-graph-conversation-state-contract.json`。

AgentLoop conversation message 与 PostgreSQL parent-table 的 Graph-only cutover 已由 commit `858bf191` 完成。live message contract 现在以显式 `scope_kind` 区分 Graph 与 standalone；Graph message 强制闭合 `run_id + graph_id/version/ref/checksum + node_id/node_instance_id + graph_checkpoint_ref + activity_id/attempt`，standalone message 禁止携带任何 Graph 字段，legacy `step_id` payload 不能进入 live reader。Local JSON 与 PostgreSQL store 均拒绝同一 conversation 的跨 Graph/checksum 或 Graph/standalone 混写；PostgreSQL migration 013 删除 parent/message active `step_id` 列和旧 run index，增加 scope constraints、parent/child fence、immutable identity trigger 与 exact Graph indexes，旧未迁移 row 保持 fail closed。Graph cursor/checkpoint 只能写入已由 exact message 建立的 Graph parent，SubAgent 也只继承 parent 已验证的 `GraphExecutionIdentity + graph_checkpoint_ref`，LLM action/普通 metadata 不能声明或提升 scope。该 slice 关闭 PRD 50.2 的 conversation message/parent-table 缺口，但不代表 6.1、6.2、6.4、6.7 或 generic Artifact identity 已完成。验证为 focused `79 passed`、mandatory smoke `2288 passed, 23 deselected, 22 warnings`、CLI Graph smoke 与 source validation `true/0/0`、change strict valid、OpenSpec strict-all `533 passed, 0 failed`。

Generic Artifact identity/storage slice 已由 commit `1b981412` 完成。`ArtifactRef` / `ArtifactWriteRequest` 现在显式区分 `graph` 与 `standalone`；Graph ref 必须闭合 `run_id + graph_id/version/ref/checksum + node_id/node_instance_id`，activity artifact 追加成对的 `activity_id + attempt`，active `step_id` 与旧 alias 在 live reader 中 fail closed。Filesystem、Local JSON index 和 PostgreSQL index 使用完整 identity key 与 node-instance canonical path/query；同一 identity 的相同 bytes/record 幂等，冲突 bytes/metadata、跨 Graph/checksum、路径篡改、symlink/reparse、旧 record shape 均拒绝；PostgreSQL migration 014 只做一次性明确 scope backfill，partial legacy rows 保持不可读并建立 immutable/index/check constraints，backup 也排除内部 `_locks`。验证为 Artifact focused `89 passed, 4 skipped`、`python -m scripts.dev compile`、mandatory smoke `2288 passed, 23 deselected, 22 warnings`、`openspec validate graph-only-orchestration --strict` 与 strict-all `533 passed, 0 failed`。该 slice 关闭 PRD 50.2 generic ArtifactRef/path/index 的模型与存储缺口，但不勾选 4.12：Graph artifact/event index 的 live writer/readers、production pointer cutover、manifest read-back/replay wiring 和剩余 Graph Artifact caller 迁移仍待完成；也不代表 3.7、4.8、6.2 或最终 Graph-only cutover 已完成。

AgentLoop exact Graph activity binding 的 inactive Gate A contract 已由 commit `afc25cc2b8d737b375e9c95b98c9ab6a1c0768b8`（tree `0e99da95a9ddfc9569cbb3057d6fe2b47cc165cb`）补齐。strict task 只允许 business inputs、conversation identity、resume flag 与 Harness 注入的 `HarnessGraphActivityTaskContext`，worker 同时 pin exact `worker_ref`/`activity_ref` 和单个 `AgentSpec`，并将 Graph run/node-instance/checkpoint ref 传给 `AgentRunner`；真实离线 `FakeLLMClient -> AgentRunner -> AgentLoop -> Graph worker -> Artifact owner -> Graph cursor/checkpoint store` 路径已覆盖。LLM call request/response 只经 Artifact owner 持久化，node candidate 仅携带二次脱敏的业务 output、bounded diagnostics/metrics、计数、trace refs、artifact refs 与 receipt checksum，不复制 raw events/trace/trajectory/tool-call payload，也不能携带 publication-shaped control fields。approval 只形成绑定完整 task-context checksum 的 typed waiting candidate evidence，不注册或恢复 Graph Wait。bundle 明确 `installs_runtime_authority=false`、serial-only、无 manifest publication/Wait authority；production resolver/composition、durable node outcome/manifest acceptance、Wait registration、conversation-state 与 Artifact commit 的幂等恢复边界、PostgreSQL parent-table cutover 和 Gate C legacy 删除仍未完成，因此 6.1、6.2、6.4-6.7 均保持开放，任务计数仍为 `26/102`。Artifact subsystem 保留且未被替换。验证为 focused `22 passed`、扩大 AgentLoop/Graph/conversation/architecture `318 passed, 4 warnings`、mandatory smoke `2688 passed, 23 deselected, 22 warnings`、source validation `true/0/0`；证据见 `evidence/agent-loop-graph-activity-binding-contract.json`。

AgentLoop approval Wait 的 Gate A contract 已由 commit `ae977438a8c4711ebba39045a4c3dae57a89356d`（tree `be7f1622c929a25ba76f7b69450c197ac2ea0329`）补齐：waiting result 作为 successful candidate 提交，v2 candidate/output 严格绑定 Graph id/version/checksum、node instance、attempt、checkpoint、scope 和 checksum；`agent_loop_wait_candidate@1` deterministic gate 在 outer state 改变前校验 exact output/evidence/Graph identity、authoritative scope 与内部一致的 checkpoint/task-context checksum lineage。`AgentLoopGraphApprovalWaitBinding` 提供 `waiting=true` 的 `Choice` condition 与 `Wait(kind=approval)` correlation/scope/signal declaration，现有 Graph evaluator/application 独占 durable `REGISTER_WAIT`，typed approval cause durable commit 后由 Harness 自动 resume；worker、AgentLoop 和 binding 本身都不注册 Wait、不恢复、不路由。focused contract/inventory 为 `20 passed`，mandatory smoke 为 `2693 passed, 23 deselected, 22 warnings`，source validation 为 `true/0/0`，OpenSpec strict-all 为 `533/533`。该 slice 仍未把 inactive AgentLoop bundle 安装到 production resolver/composition，也未完成 node outcome/terminal manifest acceptance、跨 store recovery、PostgreSQL parent identity 或 legacy runner 删除，所以 6.4 继续保持开放，任务计数仍为 `26/102`；Artifact subsystem 完整保留，证据见 `evidence/agent-loop-graph-wait-contract.json`。

AgentLoop Graph smoke 与 control-evidence slice 已完成 task 6.5/6.6。worker diagnostics 汇总 action、tool-call record 与 approval/policy issue 中的 `requested_tools`，但只由 outer `ToolAllowlistGate` 决定通过或 halt；durable cumulative-budget tests 继续证明 worker decision 不能覆盖 Harness fact，managed structured-output repair/acceptance 由独立 provider/Harness contract tests证明。`news dev run-test-agent-loop` 经 application service/composition 运行单节点 Graph preflight、physical activity、node-output/result commit、deterministic VERIFY 与 terminal manifest；内置 fake 路径固定为 3 LLM calls、1 local tool、1 judge retry、60 tokens、16 events、zero network。VERIFY 对缺失 output 或畸形 tool-policy evidence fail closed。LLM call artifacts 已修正为 Artifact owner 可接纳的 `graph-result-<64hex>` internal contract，三个 call artifacts 与一个 outcome artifact 只在 VERIFY 后进入 `GraphTerminalManifest(publication=None)`；Artifact subsystem 保留，worker/AgentLoop 无 manifest API。实现提交为 `b857688633e30e259fd4950ae77a20087906a378`（tree `54764bee9c5ab94c520c7412fd62c935a24d9a94`）；验证为 final changed-surface `55 passed`、focused `151 passed`、architecture repair `10 passed`、mandatory smoke `2695 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、change strict valid 与 strict-all `533 passed, 0 failed`；当前任务计数为 `28/102`，证据见 `evidence/agent-loop-graph-smoke-contract.json`。

- [ ] 6.1 用 Graph activity binding 取代 `AgentLoopStepRunner`，明确 `AgentRunner` 只组装依赖，AgentLoop 只执行单 Agent loop
- [ ] 6.2 将 AgentLoop LLM request/response artifact 持久化接入 artifact owner 和 Graph node outcome；AgentLoop 不得直接发布 manifest
- [x] 6.3 将 conversation cursor 和 iteration checkpoint 的 outer identity 改为 Graph run/node/checkpoint ref
- [ ] 6.4 将 approval request 的 waiting candidate 接回 Harness Graph Wait registration，禁止 AgentLoop 自己恢复或路由
- [x] 6.5 更新 AgentLoop diagnostics、retry、judge、tool policy 和 structured-output tests，证明 outer gate/budget 仍由 Harness 决定
- [x] 6.6 将 `test-agent-loop` 改为 Graph smoke fixture，检查 preflight、activity receipt、VERIFY evidence、manifest metrics 和 zero real network
- [ ] 6.7 删除 `AgentLoopStepRunner`、旧 runner registry binding、旧 Workflow smoke fixture 和仅服务它们的测试

## 7. 外部接口和 approval decision / automatic resume

- [ ] 7.1 设计并冻结 API major schema，将 `workflow_id/version/ref` 替换为 `graph_id/version/ref`，明确旧字段不在新 schema 中 alias
- [ ] 7.2 将 run response、status、inspection、replay、cancel、signal 和 approval-decision API 迁到 Graph application service；cause durable commit 后只由 Harness 自动 resume
- [ ] 7.3 将 CLI 命令和 JSON/human output 迁到 Graph identity，删除 `resume-workflow` 等旧命令，发布 approval-decision 命令并更新 help/contract tests
- [ ] 7.4 将 MCP tool、SDK method、OpenAPI schema 和 generated contract 迁到 versioned Graph approval-decision / typed Wait-cause surface
- [ ] 7.5 为 approval decision 校验当前 durable Graph run、Wait registration、approval evidence、actor identity、authorization、scope 和 idempotency；checkpoint 仅由 recovery/replay owner 校验
- [ ] 7.6 增加 interface boundary tests，证明 API/CLI/MCP/SDK 不直接构造 scheduler、executor、store 或旧 runtime
- [ ] 7.7 更新客户端调用方清单、发布说明、major cutover date 和 deprecation/deletion evidence
- [ ] 7.8 从 approval context 删除共享 `buffer_updates`、caller-supplied `node_updates` 和 `resume_metadata`；只返回 bounded Wait/approval inspection context并拒绝任何 routing/state-patch 输入

## 8. 历史数据处置

历史 Workflow records 不进入 live Graph runtime。已存在的 history-only reader/fixture 只用于识别和隔离，不能构成迁移、恢复、回放或 rollback authority。

- [x] 8.1 实现 migration-only schema readers，限定可读的 legacy versions、来源路径和输入 checksum
- [x] 8.2 实现 history-only Workflow record classifier，覆盖 manifest、events、checkpoints、replay bundles、indexes、cursor refs 和 provenance；输出只包含 source checksum、record identity 和 quarantine reason，不生成可恢复 Graph authority
- [x] 8.3 为未知 schema、缺失 Graph identity、缺失 gate evidence、非法路径、checksum mismatch、sequence gap 和 ambiguous record 定义稳定 quarantine reason codes
- [x] 8.4 实现 dry-run inventory、deterministic migration-plan checksum、idempotent rerun 和 conflict detection
- [x] 8.5 用 fixture snapshot 验证转换前后 identity、sequence、artifact containment、terminal status、gate evidence 和 replay decision 等价性，并证明 history-only checkpoint 不能被 production resume/replay/publication 路径接受
- [x] 8.6 确认 migrator 在测试中 live LLM/Tool/worker/retrieval/memory/publication call count 始终为零
- [x] 8.7 将 live legacy reader、resume、replay、worker dispatch 和 publication 统一改为 typed quarantine/fail-closed，不接受 legacy record 执行
- [ ] 8.8 将 history-only fixture 与 raw record 隔离到非 production import 路径，保留 source checksum 和 reason code
- [ ] 8.9 删除 production migration pointer、dual-store writer 和任何依赖 rollback 的切换代码
- [ ] 8.10 增加重复 legacy input、未知 schema、identity mismatch、checksum tamper 和 side-effect count 为零的 rejection tests
- [ ] 8.11 删除 active migrator/legacy reader；只保留明确标记的 history-only fixture 或审计报告
- [ ] 8.12 对 `framework`、`business`、`interfaces`、`infrastructure` 和 `scripts` 执行 import graph 检查，证明 history-only reader/classifier 不可从 production composition 到达

## 9. 删除旧 Workflow runtime

本节在 replacement owner、caller 清单和 focused tests 到位后直接执行；不依赖外部环境验收、rollback point 或观察期。

- [ ] 9.1 运行 caller-count gate，确认 `framework/workflow`、`framework/specs.workflow`、Harness Workflow namespace 和旧 runner symbols 的 production caller 全部为零
- [ ] 9.2 删除 `framework/workflow/runners` 及其 registry、executor-facing tests，确保 leaf activity tests 已迁到 Graph binding tests
- [ ] 9.3 删除 `framework/workflow/routing`、`scheduling`、`compiler` 和 `governance`，确保 Graph evaluator/scheduler/budget tests 已覆盖原行为
- [ ] 9.4 删除 `framework/workflow/checkpoint`、`buffer`、`inspection`、`operations` 和 `runtime`，确保 Graph checkpoint/event/artifact/application services 已接管
- [ ] 9.5 删除 `framework/workflow/specs`、`framework/specs/workflow.py`、Workflow registry 和仅服务旧 aggregate 的 exports
- [ ] 9.6 删除 `framework/harness/workflow` legacy namespace、`HarnessWorkflowSpec`、legacy reader/compiler、routing models 和 schema constants
- [ ] 9.7 删除 `framework/__init__.py` 和其他 root package 的 `WorkflowRunner`、`WorkflowExecutor`、legacy `RunResult` 等 exports
- [ ] 9.8 删除旧 Workflow API/CLI/MCP/SDK surface、schema writers/readers、reflection registrations 和 compatibility tests
- [ ] 9.9 删除 only-legacy fixtures；保留并标记离线 migration fixture，不允许被 production import
- [ ] 9.10 对删除后的 tracked files、imports、public symbols、registries 和 generated schemas 生成 deletion proof
- [ ] 9.11 对 control-plane flat state、SubAgent v1/v2、Workflow Event catalog/facade、Workflow Memory/Governance/Worker/Skill/LLM scope、flat Harness checkpoint/replay 和 Workflow Artifact publisher 生成逐 symbol deletion proof

## 10. Canonical OpenSpec 和文档同步

- [ ] 10.1 将本 change 的 Graph-only delta 同步到 `harness-graph` canonical spec，并移除 `Legacy Workflow Graph Compilation`
- [ ] 10.2 新增并同步 `graph-storage-indexing`、`approval-graph-resume-interfaces` 等 Graph capability，退役旧 capability requirements
- [ ] 10.3 更新 `harness-runtime`、`research-runtime`、AgentLoop、artifact、architecture、interface 和 cleanup specs，消除要求旧 runtime 的条款
- [ ] 10.4 归档 `workflow-runtime-target-closure`、`workflow-storage-indexing` 和已 superseded approval workflow capability，保留历史 provenance
- [ ] 10.5 更新架构文档、运行手册、CLI/API/MCP 文档和 Research composition 文档，统一表述为 Graph outer orchestration + AgentLoop inner loop
- [ ] 10.6 对 active source/docs/specs 执行 stale reference audit，所有剩余 Workflow 名称必须命中具体历史 allowlist
- [ ] 10.7 运行 `openspec validate graph-only-orchestration --strict` 和 `openspec validate --all --strict`
- [ ] 10.8 核对每个旧 Workflow capability 的全部 requirements 已迁入 Graph owner 或显式删除，不能只删除 capability 中的一部分 requirement

当前 Reader Repair Function worker slice 已由 commit `ce95db5ce9127aac9ad4eb151acc4d204823debd`（tree `7268a0ca7fe3b5b283a01635701aa81daea7ed97`）固化；focused `19 passed`、Reader Repair full surface `125 passed`、architecture `196 passed, 4 warnings`、mandatory smoke `2632 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533 passed, 0 failed`。这些结果不改变 3.6/3.7/5.5/5.7 的未勾选状态，证据见 `evidence/research-reader-repair-function-worker-contract.json`。

当前 Reader Repair SubAgent worker slice 已由 commit `c9f4b3f6f7e9fc3b36cac28f9b24412fa073e53b`（tree `fb44a8192161a250fa8363367c1ff771d5460630`）固化；focused `20 passed`、Reader Repair/Research Graph surface `131 passed`、architecture `196 passed, 4 warnings`、mandatory smoke `2640 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533 passed, 0 failed`。这些结果仍不改变 3.6/5.5/5.7 的未勾选状态，证据见 `evidence/research-reader-repair-subagent-worker-contract.json`。

当前 Reader Repair structured candidate adapter slice 已由 commit `9e41c7e714254bf3119e9a918ffc748763088eb3`（tree `7216849e974b3cc88d1fb9d735f21c34eae2f4ea`）固化；focused contract `50 passed`、Reader Repair/Research surface `380 passed, 3 skipped`、architecture `196 passed, 4 warnings`、mandatory smoke `2643 passed, 23 deselected, 22 warnings`、source validation `true/0/0`、OpenSpec strict-all `533 passed, 0 failed`。这些结果只关闭 provider adapter 准备缺口，不改变 3.6/5.5/5.7 的未勾选状态，证据见 `evidence/research-reader-repair-structured-candidate-contract.json`。

## 11. 验收和发布门禁

- [ ] 11.1 运行 `python -m scripts.dev compile`，修复所有 Graph namespace、import 和 schema 错误
- [ ] 11.2 运行与变更范围匹配的 Graph/Harness/Research/AgentLoop/artifact/approval/storage focused tests
- [ ] 11.3 运行 `python -m scripts.dev test` 和 `python -m scripts.dev smoke`
- [ ] 11.4 执行 production import/export/schema scan，确认旧 runtime symbol/import 为零，并确认 `workflow_id`、Workflow scope、SubAgent v1/v2 live schema、Workflow Event current registration、flat Harness state/checkpoint/replay 和 Workflow Artifact publisher 仅命中明确 history-only allowlist或完全为零
- [ ] 11.5 执行 Graph static/dynamic Research end-to-end、approval wait/resume、crash recovery、offline replay 和 artifact inspection 验收
- [ ] 11.6 检查 replay 的 live call count、publication count、memory-write count 和 tool authorization count 均符合 zero-side-effect 规则
- [ ] 11.7 检查 legacy rejection/quarantine、checksum、无 fallback、无 worker/side-effect 调用和 history-only import isolation
- [ ] 11.8 执行 release review，确认没有 compatibility facade、fallback executor、legacy writer、hidden feature flag 或未登记历史 store
- [ ] 11.9 每个职责 slice 在范围匹配的检查通过后独立提交；最终 cutover 只有在 Graph production path、zero legacy production reference、Artifact 保留约束和 mandatory smoke 全部通过后才可声明完成

Side-effect identity/storage fence slice 已由 commit `ad6195d4` 完成，并由 `351ea1f3` 修复其触发的 Artifact existence 根因。`side_effect_identity_key` 绑定 `run_id + graph_id/version/ref/checksum + origin + node_id/node_instance_id/activity_id + activity attempt + terminal_action + effect_id + scope refs`；Intent 默认 idempotency 不再只使用 `effect_id`，AttemptLease 补齐 `run_id/origin/terminal_action/activity_attempt`。InMemory 与 SQLite authoritative indexes 按完整 identity key 隔离，同一 logical `effect_id` 的不同 Graph node instances 可独立写入、重试和 fenced lease；effect-only reader 在多匹配时返回 `side_effect_identity_ambiguous`，错误 scope/idempotency fail closed；authorization、outcome、attempt generation 和 terminal/worker identity matching 均拒绝物理身份丢失。SQLite schema v3 对 v1/v2 文件一次性迁移 identity key 与旧 lease JSON，非法或缺少关联的 legacy data 保持 typed corruption。`FilesystemArtifactStore.exists()` 对缺失祖先返回 `False`，保留已存在路径组件的 symlink/reparse 拒绝，保证 TaskPlan immutable document 首次写入能到达真实 verifier。验证为 focused `110 passed, 2 skipped`，包含 SQLite v2 migration、parallel node-instance fence、TaskPlan artifact lineage；compile pass；mandatory smoke `2292 passed, 23 deselected, 22 warnings`，CLI Graph smoke 与 source validation `is_valid=true/error_count=0/warning_count=0`。该 slice 关闭 PRD 50.2 side-effect identity/storage fence 与 Artifact existence 的一项基础运行缺口，但不勾选 3.7/3.10/4.12，也不代表 live Artifact/event index、production pointer cutover、完整 Graph-only interface cutover 或最终 Graph-only release 已完成。

Generic Artifact persisted-reader strictness slice 已由 commit `9bc40457` 固化：`ArtifactRef.from_dict()` 对所有 persisted optional identity、numeric、boolean、datetime 和 metadata 字段执行 canonical type 校验，拒绝 coercion、缺失 required value、legacy alias 和未知字段；JSON artifact index/metrics 因此不会把 malformed record 变成 live Artifact authority。graph-only migration fixture 的旧 `step_id` artifact 保持 history-only，并明确验证其只能被隔离 reader 识别、不能进入 live `ArtifactRef` reader。验证为 Artifact/index/manager/metrics `100 passed, 5 skipped`、migration `22 passed`、compile pass、mandatory smoke `2292 passed, 23 deselected, 22 warnings`、AgentLoop smoke `status=succeeded`、source validation `is_valid=true/error_count=0/warning_count=0`，且 `openspec validate graph-only-orchestration --strict` 通过。本 slice 仍不勾选 `4.12`、`3.7`、`3.10` 或最终 cutover；standalone TaskPlan custom-path writer 与 live Graph Artifact/event index writer/readers、persisted pointer、manifest read-back/replay 和 production authority wiring 继续是后续生产缺口。
