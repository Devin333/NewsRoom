## 1. Field Embedding Channel Extraction

- [x] 1.1 Add OpenSpec proposal, design, spec, and task artifacts.
- [x] 1.2 Add `FieldEmbeddingChannel` under `retrieval/channels/`.
- [x] 1.3 Delegate `ResearchRetriever` field search, merge, and ranking to the channel.

## 2. Tests And Validation

- [x] 2.1 Add field channel tests for search dedupe, failure fallback, metadata merge, and ranking.
- [x] 2.2 Run retrieval tests, compile checks, and `openspec validate extract-field-embedding-channel --strict`.
