## 1. OpenSpec Setup

- [x] 1.1 Create `source-canonical-url-relative-resolution` OpenSpec change.
- [x] 1.2 Define proposal, design, tasks, and spec delta.
- [x] 1.3 Keep OpenSpec files, local state, generated outputs, and secrets out of commits.

## 2. Relative URL Canonicalization

- [x] 2.1 Add optional `base_url` support to `canonicalize_url`.
- [x] 2.2 Add optional `base_url` support to `source.normalize_url`.

## 3. Validation

- [x] 3.1 Add focused canonicalization and tool tests.
- [x] 3.2 Run OpenSpec validation and focused tests.
- [x] 3.3 Run full tests, diff checks, secret scan, and commit.
