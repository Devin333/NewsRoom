## 1. Specification

- [x] 1.1 Validate the OpenSpec change artifacts.

## 2. Live Answer Evaluation

- [x] 2.1 Add `--live-answer-eval` and mode conflict validation to `run_evidence_eval`.
- [x] 2.2 Convert gated answer payloads into `EvidenceAnswerSample` rows.
- [x] 2.3 Record `answer_eval_mode` metadata in evidence reports.

## 3. CI Labeling

- [x] 3.1 Rename deterministic CI promotion check labels to make the pipeline-only scope explicit.

## 4. Tests

- [x] 4.1 Add tests for live answer sample conversion and answer mode metadata.
- [x] 4.2 Add tests for conflicting answer mode validation and deterministic CI labels.
