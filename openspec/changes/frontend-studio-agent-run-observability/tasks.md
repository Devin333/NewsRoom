## 1. Setup

- [x] 1.1 Confirm `frontend` already has React Flow and Zustand dependencies.
- [x] 1.2 Import React Flow CSS and add Studio navigation entries.

## 2. Data Layer

- [x] 2.1 Add Studio observability types and run inspector store.
- [x] 2.2 Add mock run data that satisfies Task 04 coverage requirements.
- [x] 2.3 Add server-side API adapters that merge `/api/v1/runs*` data with mock fallback data.

## 3. Studio UI

- [x] 3.1 Implement `/studio` overview dashboard.
- [x] 3.2 Implement `/studio/runs` table, toolbar, filtering, sorting, status badges, and empty state.
- [x] 3.3 Implement `/studio/runs/[runId]` header, DAG/timeline, step detail, inspector tabs, artifacts, quality, and error views.

## 4. Verification

- [x] 4.1 Validate OpenSpec change.
- [x] 4.2 Run frontend typecheck, lint, build, and repository web-check; fix blocking issues.
